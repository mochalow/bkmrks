# Зависимости для разработки: тесты, покрытие, линтер.
# Отдельно от requirements.txt, чтобы прод-образ не тащил тестовый инструментарий.
#
# Установка:  uv pip install -r requirements-dev.txt
#         или pip install -r requirements-dev.txt
-r requirements.txt

pytest==9.1.1
# httpx2, а не httpx: starlette 1.3.1 предупреждает, что связка
# с httpx устарела, и просит httpx2. Нужен для fastapi.testclient.
httpx2==2.12.0
pytest-cov==7.1.0
ruff==0.16.3

# Свойства вместо примеров: идемпотентность нормализации URL и тега
# проверяется на генерируемых данных, а не на трёх строках таблицы.
hypothesis==6.167.1
# Случайный порядок тестов - ловит скрытые зависимости между ними.
pytest-randomly==4.1.0
# Дымовые E2E в браузере. После установки нужен один раз:
#     python -m playwright install chromium
# Без браузера сценарии пропускаются с причиной, набор остаётся зелёным.
pytest-playwright==0.9.0
